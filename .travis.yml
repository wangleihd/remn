language: node_js

node_js:
  - 6

script:
  - npm start

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install make g++ python -y
  - sudo npm install node-gyp -g

notifications:
  email:
    on_failure: change

cache:
  yarn: true
  directories:
    - node_modules
    
services:
  mongodb
