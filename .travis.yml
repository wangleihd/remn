language: node_js

node_js:
  - 6

script:
  - npm start

before_install:
  - apt-get install python
  - apt-get install make
  - apt-get install g++
  - npm install node-gyp -g

notifications:
  email:
    on_failure: change

cache:
  yarn: true
  directories:
    - node_modules
    
services:
  mongodb
